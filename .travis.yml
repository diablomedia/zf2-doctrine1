language: php

php:
  - 5.6
  - 7.0
  - 7.1
  - 7.2
  - nightly
  - hhvm

before_script:
  - composer self-update
  - composer update --prefer-source

script:
  - ./vendor/bin/phpunit

matrix:
  allow_failures:
    - php: hhvm
    - php: nightly
